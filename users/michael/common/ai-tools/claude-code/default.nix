{
  lib,
  pkgs,
  ...
}:
let
  # Auto-discover command files
  commandFiles = builtins.readDir ./commands;
  commandEntries = lib.filterAttrs (
    name: type: type == "regular" && lib.hasSuffix ".md" name
  ) commandFiles;
  commandFileAttrs = lib.mapAttrs' (
    name: _: lib.nameValuePair ".claude/commands/${name}" { source = ./commands/${name}; }
  ) commandEntries;

  # Auto-discover hook files
  hooksDir = ./hooks;
  hookFiles = builtins.readDir hooksDir;
  importHook = name: import (hooksDir + "/${name}") { inherit pkgs; };
  hooks = lib.foldl' (acc: name: acc // (importHook name)) { } (
    lib.filter (name: lib.hasSuffix ".nix" name) (builtins.attrNames hookFiles)
  );

  # Auto-generated "Available Tools" inventory, appended to CLAUDE.md.
  # Reads descriptions from each file's frontmatter so it can never drift.
  mdFilesIn =
    dir:
    builtins.attrNames (
      lib.filterAttrs (name: type: type == "regular" && lib.hasSuffix ".md" name) (builtins.readDir dir)
    );

  frontmatterField =
    field: path:
    let
      line = lib.findFirst (l: lib.hasPrefix "${field}: " l) null (
        lib.splitString "\n" (builtins.readFile path)
      );
    in
    if line == null then
      ""
    else
      lib.removePrefix "\"" (lib.removeSuffix "\"" (lib.removePrefix "${field}: " line));

  agentEntry =
    name:
    "- **${lib.removeSuffix ".md" name}** — ${frontmatterField "description" (./agents + "/${name}")}";

  commandEntry =
    name:
    let
      hint = frontmatterField "argument-hint" (./commands + "/${name}");
      hintStr = if hint == "" then "" else " `${hint}`";
    in
    "- **/${lib.removeSuffix ".md" name}**${hintStr} — ${
      frontmatterField "description" (./commands + "/${name}")
    }";

  skillEntry =
    name: "- **${name}** — ${frontmatterField "description" (./skills + "/${name}/SKILL.md")}";
  skillDirs = builtins.attrNames (
    lib.filterAttrs (_name: type: type == "directory") (builtins.readDir ./skills)
  );

  toolsInventory = ''

    <!-- BEGIN generated by Nix from agents/, commands/, skills/ — edit those files, not this block -->

    ## Available Tools

    ### Agents (invoke explicitly)

    ${lib.concatMapStringsSep "\n" agentEntry (mdFilesIn ./agents)}

    ### Commands

    ${lib.concatMapStringsSep "\n" commandEntry (mdFilesIn ./commands)}

    ### Skills (load automatically when relevant)

    ${lib.concatMapStringsSep "\n" skillEntry skillDirs}

    <!-- END generated -->
  '';

  claudeMd = pkgs.writeText "CLAUDE.md" (builtins.readFile ./CLAUDE.md + toolsInventory);
in
{
  home.file = lib.mkMerge [
    commandFileAttrs
    {
      ".claude/CLAUDE.md".source = claudeMd;
      ".claude/skill-rules.json".source = ./skill-rules.json;
      ".claude/agents".source = ./agents;
      ".claude/skills".source = ./skills;
    }
  ];

  programs.claude-code = {
    enable = true;
    mcpServers = {
      figma-desktop = {
        type = "http";
        url = "http://127.0.0.1:3845/mcp";
      };
      playwright = {
        command = "npx";
        args = [ "@playwright/mcp@latest" ];
      };
      # Nova MCP servers (via Tailscale ingress on Zenith K3s)
      ticktick = {
        type = "http";
        url = "https://nova-mcp-hub.tail575dda.ts.net/ticktick/mcp";
      };
      google-workspace = {
        type = "http";
        url = "https://nova-mcp-hub.tail575dda.ts.net/google-workspace/mcp";
      };
      paprika = {
        type = "http";
        url = "https://nova-mcp-hub.tail575dda.ts.net/paprika/mcp";
      };
      ddg-search = {
        type = "http";
        url = "https://nova-mcp-hub.tail575dda.ts.net/ddg-search/mcp";
      };
      nova-planner = {
        type = "http";
        url = "https://nova-planner.tail575dda.ts.net/mcp";
      };
    };
    settings = {
      theme = "dark";
      inherit hooks;
      permissions = {
        allow = [
          # Safe read-only git commands
          "Bash(git add:*)"
          "Bash(git status)"
          "Bash(git log:*)"
          "Bash(git diff:*)"
          "Bash(git show:*)"
          "Bash(git branch:*)"
          "Bash(git remote:*)"

          # Safe Nix commands (mostly read-only)
          "Bash(nix:*)"

          # Safe file system operations
          "Bash(ls:*)"
          "Bash(find:*)"
          "Bash(grep:*)"
          "Bash(rg:*)"
          "Bash(cat:*)"
          "Bash(head:*)"
          "Bash(tail:*)"
          "Bash(mkdir:*)"
          "Bash(chmod:*)"

          # Safe system info commands
          "Bash(systemctl list-units:*)"
          "Bash(systemctl list-timers:*)"
          "Bash(systemctl status:*)"
          "Bash(journalctl:*)"
          "Bash(dmesg:*)"
          "Bash(env)"
          "Bash(claude --version)"
          "Bash(nh search:*)"

          # Core Claude Code tools
          "Glob(*)"
          "Grep(*)"
          "LS(*)"
          "Read(*)"
          "Search(*)"
          "Task(*)"
          "TodoWrite(*)"

          # Safe web fetch from trusted domains
          "WebFetch(domain:github.com)"
          "WebFetch(domain:raw.githubusercontent.com)"
        ];
        ask = [
          # Potentially destructive git commands
          "Bash(git reset:*)"
          "Bash(git commit:*)"
          "Bash(git push:*)"
          "Bash(git pull:*)"
          "Bash(git merge:*)"
          "Bash(git rebase:*)"
          "Bash(git checkout:*)"
          "Bash(git switch:*)"
          "Bash(git stash:*)"

          # File deletion and modification
          "Bash(rm:*)"
          "Bash(mv:*)"
          "Bash(cp:*)"

          # System control operations
          "Bash(systemctl start:*)"
          "Bash(systemctl stop:*)"
          "Bash(systemctl restart:*)"
          "Bash(systemctl reload:*)"
          "Bash(systemctl enable:*)"
          "Bash(systemctl disable:*)"
          "Bash(systemctl mask:*)"
          "Bash(systemctl unmask:*)"

          # Network operations
          "Bash(curl:*)"
          "Bash(wget:*)"
          "Bash(ping:*)"
          "Bash(ssh:*)"
          "Bash(scp:*)"
          "Bash(rsync:*)"

          # Package management
          "Bash(sudo:*)"
          "Bash(nixos-rebuild:*)"

          # Process management
          "Bash(kill:*)"
          "Bash(killall:*)"
          "Bash(pkill:*)"
        ];
        deny = [
          # Sensitive files - secrets, credentials, keys
          "Read(.env)"
          "Read(.env.*)"
          "Read(**/secrets/*)"
          "Read(**/*credential*)"
          "Read(**/*.pem)"
          "Read(**/*.key)"
        ];
        defaultMode = "default";
      };
      cleanupPeriodDays = 365;
      verbose = true;
      includeCoAuthoredBy = false;

      # Plugins
      extraKnownMarketplaces = {
        "claude-notifications-go" = {
          source = {
            source = "github";
            repo = "777genius/claude-notifications-go";
          };
        };
        "minimalist-entrepreneur" = {
          source = {
            source = "github";
            repo = "slavingia/skills";
            path = ".claude-plugin/marketplace.json";
          };
        };
        "ponytail" = {
          source = {
            source = "github";
            repo = "DietrichGebert/ponytail";
          };
        };
      };
      enabledPlugins = {
        "claude-notifications-go@claude-notifications-go" = true;
        "minimalist-entrepreneur@minimalist-entrepreneur" = true;
        "ponytail@ponytail" = true;
        "typescript-lsp@claude-plugins-official" = true;
        "context7@claude-plugins-official" = true;

        "explanatory-output-style@claude-plugins-official" = true;
        "frontend-design@claude-plugins-official" = true;
      };

      statusLine = {
        type = "command";
        command = "input=$(cat); echo \"[$(echo \"$input\" | jq -r '.model.display_name')] 📁 $(basename \"$(echo \"$input\" | jq -r '.workspace.current_dir')\")\"";
        padding = 0;
      };
    };
  };
}
